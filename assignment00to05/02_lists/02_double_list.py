def main():
    numbers = [1, 2, 3, 4]  # Creates a list of numbers
    numbers = [num * 2 for num in numbers]  # Doubles each element in the list
    print(numbers)  # This should print the doubled list

# There is no need to edit code beyond this point
if __name__ == '__main__':
    main()
# The code above creates a list of numbers, doubles each element, and prints the result.